const isLoggedIn = true;
const hasAdminAccess = true;

if (isLoggedIn) {
  if (hasAdminAccess) {
    console.log("Welcome, admin.");
  }
}

//This is only so you can understand how it works, when possible resort to && isntead of nested if statements.